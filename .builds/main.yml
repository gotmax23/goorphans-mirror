---
image: fedora/latest
sources:
  - https://codeberg.org/gotmax23/goorphans
packages:
  - golang
  - krb5-devel
tasks:
  - build: |
      cd goorphans
      go build .
  - test: |
      cd goorphans
      go test ./...
